---
title: while dövrü
description: Şərt doğru olduqca təkrarla — və sonsuz dövr tələsindən qaçmaq.
sidebar:
  order: 3
---

import PyRunner from '../../../../../components/python/PyRunner.astro';
import Mission from '../../../../../components/python/Mission.astro';
import ModuleProgress from '../../../../../components/python/ModuleProgress.astro';

`for` neçə dəfə təkrarlayacağını **əvvəlcədən** bilir. Bəs bilmirsənsə? Oyunda «düz cavab tapılana qədər soruş» — neçə dəfə olacaq, heç kim bilmir. Bunun üçün `while` var: *«nə qədər ki şərt doğrudur — təkrarla»*.

## while necə işləyir

<PyRunner
	id="ex-while"
	starterCode={`i = 1\nwhile i <= 5:\n    print(f"Təkrar {i}")\n    i = i + 1\nprint("Bitdi!")`}
	height="7rem"
/>

Üç vacib hissə:

1. **Başlanğıc:** `i = 1` — sayğac dövrdən əvvəl yaradılır
2. **Şərt:** `while i <= 5:` — hər təkrardan əvvəl yoxlanılır
3. **Dəyişiklik:** `i = i + 1` — sayğac irəliləyir, yoxsa şərt heç vaxt yanlış olmaz!

## ⚠️ Sonsuz dövr tələsi

3-cü hissəni unutsan nə olar? Şərt **həmişə** doğru qalar və dövr **heç vaxt bitməz**. Yoxla — qorxma, bizim meydança 5 saniyədən sonra onu özü dayandırır:

<PyRunner
	id="ex-sonsuz"
	starterCode={`i = 1\nwhile i <= 5:\n    print("Kömək, dayanmıram! 😱")\n    # i-ni artırmağı "unutmuşuq"`}
	height="6rem"
/>

Gördün? `i` heç vaxt 5-i keçmir, çünki onu artıran yoxdur. Öz kompüterində belə proqram **əbədi** işləyərdi — ona görə `while` yazanda həmişə özündən soruş: *«bu dövr nə vaxtsa bitəcək?»*

## Missiyalar

<Mission
	id="m03-l03-m1"
	xp={10}
	title="Geriyə sayma"
	starterCode={`# 5-dən 1-ə geri say, sonda "Start!" çap et\n# 5\n# 4\n# ...\n# 1\n# Start!\n`}
	checkCode={`_lines = [l.strip() for l in _stdout.strip().splitlines() if l.strip()]\nassert _lines[:5] == ["5", "4", "3", "2", "1"], "HINT:i = 5-dən başla, while i >= 1: içində print(i) və i = i - 1."\nassert "start" in _stdout.lower(), "HINT:Dövr bitəndən sonra (girintisiz!) print(\\"Start!\\") yaz."`}
	inputs={[]}
>
	Raket buraxılışı: `while` ilə 5-dən 1-ə geri say, dövr bitəndən sonra `Start!` çap et.
</Mission>

<Mission
	id="m03-l03-m2"
	xp={15}
	title="İkiqat-ikiqat"
	starterCode={`eded = 1\n# Ədədi 2-yə vur-vur, 1000-i keçən kimi dayan\n# və həmin ədədi çap et\n`}
	checkCode={`assert "1024" in _stdout, "HINT:while eded <= 1000: içində eded = eded * 2, dövrdən sonra print(eded). Cavab 1024 olmalıdır."`}
	inputs={[]}
>
	1-dən başla, hər dəfə 2-yə vur: 1, 2, 4, 8... 1000-i keçən **ilk** ədədi tap. (Tanış rəqəm çıxacaq! 😉)
</Mission>

<Mission
	id="m03-l03-m3"
	xp={20}
	title="Ədədi tap oyunu"
	starterCode={`gizli = 7\ncavab = int(input("Ədədi tap (1-10): "))\n# Nə qədər ki cavab gizli ədədə bərabər deyil,\n# yenidən soruş. Tapanda: "Tapdın! 🎉"\n`}
	checkCode={`assert "tapdın" in _stdout.lower() or "tapdin" in _stdout.lower(), "HINT:while cavab != gizli: içində cavab = int(input(...)) — Daxiletmələrdə 3 cəhd var, üçüncüsü düzdür."`}
	inputs={['5', '9', '7']}
>
	İlk **əsl oyununu** yaz! Gizli ədəd `7`-dir. Oyunçu tapana qədər `while` soruşmağa davam etsin, tapanda `Tapdın! 🎉` desin. (Daxiletmələrdə oyunçunun 3 cəhdi hazırdır: `5`, `9`, `7`.)
</Mission>

<ModuleProgress
	moduleId="03"
	missions={['m03-l01-m1', 'm03-l01-m2', 'm03-l01-m3', 'm03-l02-m1', 'm03-l02-m2', 'm03-l02-m3', 'm03-l03-m1', 'm03-l03-m2', 'm03-l03-m3']}
	badgeLabel="Təkrarla nişanı"
/>

---

🏅 **Bu modulu bitirdin!** Artıq əsl oyun da yazmısan. **Növbəti modulda** proqramına yaddaş qutuları əvəzinə bütöv **siyahılar** verəcəyik — `list` və `dict` ilə yüzlərlə məlumatı bir yerdə saxlamaq. [Davam et →](../../04-kolleksiyalar/siyahilar/)
